import { ToastProps } from "../types/index.js"

/**
 * Toast component
 * @param param0 Props of the toast
 * @returns 
 */
export default function Toast({ data }: ToastProps) {
  return <div>{/** Something here **/}</div>;
}
