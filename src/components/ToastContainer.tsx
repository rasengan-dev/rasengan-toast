import { ToastContainerProps } from "../types";

/**
 * ToastContainer component
 * @returns JSX.Element
 */
export default function ToastContainer(props: ToastContainerProps) {
  return <section {...props}>{/* Add something here */}</section>;
}
